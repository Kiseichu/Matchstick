name: Matchstick Ci/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu
    steps:
    -name: Compiled program work
    run: make
    -name: program test
    run: ./matchstick 5 5
    -name: run fclean
    run: make fclean

